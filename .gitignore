# Exports locais do Mongo (mongoexport --jsonArray) usados pelo backend
# DATA_BACKEND="json_export" -- dados reais de terceiros, não versionar.
dbs/*.json

# Ambiente virtual Python
.venv/
__pycache__/
